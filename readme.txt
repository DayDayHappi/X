C++ 重复定义问题
头文件重复包含时，会出现redefintion ,例如在 c.h中已经包含了 b.h ，在main.cpp中只包含 c.h就可以了
如果再包含b.h 则会重复定义。

C++继承时调用父类构造
如果在父类中只定义了有参构造，子类继承父类后，如果没有显式调用父类的有参构造，则在实例化子类的对象时，
会出现"undefined reference" ,因为编译器会默认调用父类的默认构造，也就是无参构造。
此时需要我们去显示的调用父类的有参构造函数。
